{
  "name": "servicenow-anywhere",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "build": "bun build ./src/bundle.js --outfile=./dist/bundle.js && bun run copy-assets.js"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0-rc.10"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0-rc.4"
  }
}
